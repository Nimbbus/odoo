<odoo>

<record id="estate_property_view_form" model="ir.ui.view">
    <field name="name">estate.property.form</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <form string="Property">
         <header>
                <button name="action_sold" type="object" string="Sold" invisible="state != 'offer_accepted'"/>
                <button name="action_cancel" type="object" string="Cancel" invisible="state == 'cancelled' or state == 'sold' "/>
                <field name="state"  readonly="True" widget="statusbar"  statusbar_visible="new,offer_received,offer_accepted,sold"/>
            </header>
            <sheet>
             
                <div class="oe_title">
                    <h1>
                        <field name="name"/>
                    </h1>
                </div>
                <group>
                  <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" editable="bottom"/>
                </group>
               
                <group>
                    <group>
                        <field name="property_type_id"/>
                        <field name="postcode"/>
                        <field name="date_availability" string="Available From" optional="hide"/>
                    </group>
                    <group>
                        <field name="expected_price"/>
                        <field name="best_price" font_weight="bold"/>
                        <field name="selling_price"/>
                    </group>
                </group>

                

               
                <notebook>
                    <page string="Description">
                        <group>
                            <field name="description"/>
                            <field name="bedrooms"/>
                            <field name="living_area" string="Living Area (sqm)"/>
                            <field name="facades"/>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" string="Garden Area (sqm)" invisible= "not garden"/>
                             <field name="total_area" font_weight="bold" invisible= "not garden"/>
                            <field name="garden_orientation" invisible= "not garden"/>
                            <field name="active" />
                        </group>
                    </page>
                    <page string="Offers">
                        <field name="offer_ids" readonly="state in ('cancelled','sold','offer_accepted')">
                            <list string="Offers" editable="bottom">
                                <field name="price"/>
                                <field name="partner_id" decoration-danger="status == 'refused'" decoration-success="status == 'accepted'"/>
                                <field  name="validity"/>
                                <field name="date_deadline"/>
                                <button name="action_accept" type="object" icon="fa-check" title="Accept" invisible="status"/>
                                <button name="action_refuse" type="object" icon="fa-times" title="Refuse" invisible="status"/>
                                <field name="status" readonly="True"
                                decoration-danger="status == 'refused'" decoration-success="status == 'accepted'" invisible="1"/>
                            </list>
                        </field>
                    </page>
                    <page string= "other info">
                        <group>
                            <field name="seller_id"/>
                            <field name="buyer_id"/>
                        </group>
                    </page>
                </notebook>
            </sheet>
        </form>
    </field>
</record>

<record id="view_estate_property_list" model="ir.ui.view">
    <field name="name">estate.property.list</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <list string="Properties">
            <field name="name"
            decoration-success="state == 'offer_accepted' or state == 'offer_received'"
            decoration-bf="state == 'offer_accepted'"
            decoration-muted="state == 'sold' or state == 'cancelled'"/>
            <field name="property_type_id" options="{'no_create': True, 'no_open': True}"/>
            <field name="postcode"/>
            <field name="bedrooms"/>
            <field name="expected_price"/>
            <field name="selling_price"/>
            <field name="state"/>
        </list>
    </field>
</record>

<record id="view_estate_property_search" model="ir.ui.view">
    <field name="name">estate.property.search</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <search string="Properties">
            <filter name="available" string="Available" domain="[('state','=','new')]"/>
            <field name="name"/>
            <field name="property_type_id"/>
            <field name="state"/>
            <field name="living_area" filter_domain="[('living_area','>=',self)]"/>
        </search>
    </field>
</record>

<record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="view_id" ref="view_estate_property_list"/>
        <field name="search_view_id" ref="view_estate_property_search"/>
</record>

<record id="estate_property_kanban" model="ir.ui.view">
    <field name="name">estate.property.kanban</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
        <kanban default_group_by="state" groups_draggable="false" records_draggable="false">
            <field name="state"/>
            <templates>
                <t t-name="card">
                    <div class="oe_kanban_global_click">
                        <div>
                         <strong><field name="name"/></strong>
                        </div>
                        <div t-if="record.state.raw_value == 'offer_received'">
                         <strong>Best Price:</strong> <field name="best_price" />
                        </div>
                        <div>
                         <strong>Expected Price:</strong> <field name="expected_price" />
                        </div>
                        <div t-if="record.state.raw_value == 'offer_accepted'">
                         <strong>Selling Price:</strong> <field name="selling_price" />
                        </div>
                        <div>
                            <strong>Tags:</strong> <field name="tag_ids"/>
                        </div>
                    </div>
                </t>
            </templates>
        </kanban>
    </field>
</record>




</odoo>
